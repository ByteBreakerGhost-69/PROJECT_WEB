# Opsional — isi kalau mau pakai Supabase Auth beneran.
# Kalau dikosongkan, auth otomatis fallback ke simulasi localStorage
# (lihat lib/local-auth.ts) supaya tetap bisa dites tanpa backend.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
